<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:background="#ADD8E6"> <!-- Fondo azul clarito -->

    <!-- Logo de la aplicación -->
    <ImageView
        android:id="@+id/logoApp"
        android:layout_width="208dp"
        android:layout_height="155dp"
        android:src="@drawable/raza"
        android:layout_marginBottom="32dp" /> <!-- Espacio debajo de la imagen -->

    <!-- Campo para el nombre de usuario -->
    <EditText
        android:id="@+id/nombreUsuario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nombre de usuario"
        android:inputType="text" />

    <!-- Campo para la clave del usuario -->
    <EditText
        android:id="@+id/claveUsuario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Clave"
        android:inputType="textPassword" />

    <!-- Botón para iniciar sesión con bordes cuadrados y tamaño original -->
    <Button
        android:id="@+id/botonIniciarSesion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Iniciar Sesión"
        android:textColor="#808080"
        android:backgroundTint="#94FD98"
        android:radius="0dp"
        android:layout_marginTop="32dp" />
</LinearLayout>
